// functioner 1

// function safeMultiply(x, y) {

    // console.log("tal " + x + " gange med tal " + y, "er lig med " + (x * y));
// }
// safeMultiply(13, 7);
// safeMultiply(3, 51);


// functioner 2
// function Multiply(x, y) {                                                        // "function" "argument/navn på function" "(element, element"{
// if (isNaN(x) || isNaN(y)) {                                                      // "if" stiller spørgsmålet om hvor vidt "x" og "y" er nr
// return "er ikke et nummer";                                                      // hvis "x" og "y" ikke er numre printes denne kode
// }
// else {
// return x * y;                                                                    // hvis "x" og "y" ER numre, printes denne kode
// }
// }

// console.log(Multiply(13, 2))                                                     // *console.log("argument/navn på function"(element, element))


// function 3
// function Multiply(x, y) {
    // if (x != undefined && y != undefined) {                                      // tjekker om argumenterne har indhold
        // if (isNaN(x) || isNaN(y)) {
            // return "er ikke et nummer";
        // }
        // else {
            // return x * y;
        // }
    // }
    // else {
        // return -1;
    // }
// }

// console.log(Multiply(13, 7))